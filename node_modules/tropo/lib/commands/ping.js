var cli = require('../cli'),
    fs = require('fs'),
    repo = require('../repo'),
    util = require('util');

var log = cli.log;

module.exports = function (arg, callback) {
    repo.Provisioning.find(undefined, function (err, data) {
        if (err) {
            // log.data(err);
        }
        if (data) {
            // log.data(util.inspect(data));
        }
        callback(err, data);
    });
};

