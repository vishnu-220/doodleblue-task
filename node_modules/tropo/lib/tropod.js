var cli = require('../lib/daemon/daemon');

cli.start(function (err) {
	function onexit (code, status) {
		if (err) {
  			process.removeListener('exit', onexit);
  			process.exit(1);
		}
	}

	process.on('exit', onexit);
});

