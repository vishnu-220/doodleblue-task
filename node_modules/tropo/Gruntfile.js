module.exports = function(grunt) {

  grunt.initConfig({
    jshint: {
      files: ['Gruntfile.js', 'lib/**/*.js']
    },
    nodeunit: {
        all: ['test/*.js'],
        options: {
            reporter: 'junit',
            reporterOptions: {
                output: 'junit.out'
            }
        }
    },
    bfdocs: {
        documentation: {
            options: {
                title: 'Tropo.js Documentation',
                manifest: 'manifest.json',
                dest: 'apidocs/',
                theme: 'default'
            }
        }
    },
    run: {
        pack: {
          cmd: 'npm',
          args: [
            'pack'
          ]
        }
    }
  });

  grunt.loadNpmTasks('grunt-contrib-jshint');
  grunt.loadNpmTasks('grunt-contrib-nodeunit');
  grunt.loadNpmTasks('grunt-beautiful-docs');
  grunt.loadNpmTasks('grunt-run');
  
  grunt.registerTask('default', ['jshint','nodeunit', 'bfdocs']);
};